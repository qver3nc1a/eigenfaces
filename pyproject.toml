[project]
name = "eigenfaces"
version = "0.1.0"
description = ""
authors = [
    {name = "Iliana Dulina",email = "ilyana.dulina07@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=1.26",
    "pillow (>=12.3.0,<13.0.0)",
]

[tool.pytest.ini_options]
pythonpath = ["src"]

[tool.poetry.group.dev.dependencies]
pytest = "^8.0.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"